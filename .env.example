# Chacadom site — build-time environment (public values only, static site).
# Base path for asset URLs. Default targets GitHub Pages (/chacadom/).
# Netlify sets VITE_BASE=/ automatically via netlify.toml.
VITE_BASE=/chacadom/
